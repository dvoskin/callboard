# Zoho OAuth credentials
# Get these from https://api-console.zoho.com
ZOHO_CLIENT_ID=your_client_id_here
ZOHO_CLIENT_SECRET=your_client_secret_here
ZOHO_REFRESH_TOKEN=your_refresh_token_here

# Region-specific URLs (US is default — change for EU/AU/etc)
ZOHO_BASE_URL=https://www.zohoapis.com
ZOHO_ACCOUNTS_URL=https://accounts.zoho.com

# How many minutes off from scheduled time still counts as "on time" (default 15)
SCHEDULED_TOLERANCE_MINUTES=15

# Zoho Books (Quotes Sent panel) — leave blank to disable the panel.
# Org ID is from https://books.zoho.com/app/<org_id>/dashboard
# Refresh token must include ZohoBooks.estimates.READ scope; if absent the
# Books client falls back to ZOHO_REFRESH_TOKEN (which must then have the
# Books scope added).
ZOHO_BOOKS_ORG_ID=
ZOHO_BOOKS_REFRESH_TOKEN=
